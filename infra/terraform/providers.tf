# Authentication uses your environment or shared credentials file.
# For CI (Jenkins), configure AWS credentials via environment variables.
#
# TODO: Ensure the Jenkins agent has AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
# and AWS_DEFAULT_REGION or uses a role with appropriate permissions.
